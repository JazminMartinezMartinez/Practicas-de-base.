#include "iostream"
using namespace std;
int main (){
  flout lado1,lado2,lado3
  cout<<"Digita el lado uno del triangulo";
  cin>>lado1;
  cout<<"Digita el lado dos del triangulo";
  cin>>lado2;
  cout<<"Digita el lado tres del triangulo";
  cin>>lado3;
  
   if(lado1 == lado2 and lado1 == lado3 ==lado2 and lado1 == lado3);
  cout<<"Tu triangulo es equilatero";

  else if( lado2 == lado1 or lado2 == lado3 or lado1 == lado 3);
  cout<<"Tu triangulo es isoceles";

  else if(lado1=! lado2 and lado2=! lado3 and lado=! lado3);
  cout<<"Tu triangulo es escaleno";

  else{
    cout<<"Tu no tienes un triangulo";
    {
      return 0;
      {
